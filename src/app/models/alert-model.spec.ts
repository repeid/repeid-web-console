/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {AlertModel} from './alert-model';

describe('AlertModel', () => {
  it('should create an instance', () => {
    expect(new AlertModel()).toBeTruthy();
  });
});
